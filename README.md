# AI-DermalScan
A deep learning-based system that can detect and  classify  facial aging signs—such as wrinkles, dark spots, puffy eyes, and clear skin—using a  pretrained  EfficientNetB0 model. The system integrates preprocessing, data augmentation, and  classification into  a backend pipeline, with a web-based frontend for user interaction.
